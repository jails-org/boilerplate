{
  "name": "@whitelabel/www",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "webpack-dev-server --env mode=development --port 3000 --host 0.0.0.0 --config ./config/webpack.config.ts",
    "build": "ENV=production webpack --env mode=production --config ./config/webpack.config.ts",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.24.0",
    "vanilla-lazyload": "^17.8.3"
  },
  "devDependencies": {
    "@whitelabel/packages": "1.0.0"
  }
}
